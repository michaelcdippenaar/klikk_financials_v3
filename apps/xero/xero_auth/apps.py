from django.apps import AppConfig


class XeroAuthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.xero.xero_auth'
    verbose_name = 'Xero Authentication'

